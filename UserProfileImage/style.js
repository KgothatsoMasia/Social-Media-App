import {StyleSheet} from "react-native";

const profileStyle = StyleSheet.create({
  userImageContainer: {
    borderColor: '#F35BAC',
    padding: 4,
    borderWidth: 2,
  },
});

export default profileStyle;